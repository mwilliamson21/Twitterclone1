from twitterclone.notifications.models import *
from twitterclone.tweets.models import *
from twitterclone.twitterusers.models import *

"""
Django doesn't want to find models that aren't located in the project
root. This is just to put references to the models in a place where
Django can find them.

~ Joe
"""
